# Workflow

1. Intake task into `taskboard/backlog.md`.
2. Run `tools/run_pipeline.sh` with task statement.
3. Review outputs in `taskboard/in-progress.md` and `logs/`.
4. Human operator approves/rejects.
5. Move complete entry to `taskboard/done.md`.
