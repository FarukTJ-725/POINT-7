# Branching and PRs

- Branch naming: `feature/<area>-<short-desc>` or `fix/<area>-<short-desc>`.
- PR must include scope, risk, test evidence, rollback note.
- Require human operator approval before merge to protected branch.
