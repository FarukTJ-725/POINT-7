# Core Rules — QA

1. Never expose secrets; only reference env var names.
2. Keep evidence in `taskboard/in-progress.md` and `logs/`.
3. Respect workspace boundaries and avoid unauthorized cross-scope edits.
4. Escalate blockers immediately with clear owner and next action.
