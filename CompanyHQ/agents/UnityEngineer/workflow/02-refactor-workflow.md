# Refactor Workflow

1. Baseline metrics (build time, memory, frame time, test pass).
2. Isolate one subsystem and refactor behind compatibility seam.
3. Regression validate + benchmark compare.
4. Rollout in phases with rollback marker commits.
